  <%= semantic_form_for @company, :html => {:multipart => true} do |f| %>
    <%= f.inputs :name, :phone, :address, :contact_person, :site, :notes %>
<%= f.input :services, :as => :check_boxes  %>
<%= f.semantic_fields_for :pictures do |ff| %>
      <%= ff.input :image, as: :file %>
      <% end  %>
      <%  %>
    <%= f.actions %>
  <% end %>

<hr>
<hr>
